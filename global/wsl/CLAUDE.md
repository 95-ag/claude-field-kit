# Global CLAUDE.md — WSL (no overlay)

> WSL uses `global/shared/CLAUDE.md` as-is — there is no WSL-specific delta. The only cross-environment
> difference is Windows' extra `@rules/windows-claude.md` import (see `global/windows/CLAUDE.md`). Add an
> overlay here only if a WSL-specific rule or instruction ever appears.
