# Global CLAUDE.md — Windows overlay

> Composes with `global/shared/CLAUDE.md`; the only Windows delta is the extra rule import below.
> On sync, Windows `~/.claude/CLAUDE.md` = shared body + this import, and `~/.claude/rules/` also
> receives `windows-claude.md`. WSL needs no overlay.

@rules/windows-claude.md
