# Tasks — <Project>

> Live checklist, not an archive — Done holds the current phase + last 1–2 phases (crisp); prune older.
> Datestamp Done / Deferred entries — ISO `YYYY-MM-DD`. Phase state + decisions → `session.md`.

## Now

<!-- Group active work into clusters (a phase, a feature, a plan's steps). Name a cluster's plan inline;
     multiple plans can be active at once (e.g. a sub-plan within a larger pipeline plan). -->

### <Cluster / phase> — plan: `<path>`
- [ ] <…>

## Open decisions
<!-- Unresolved choices that block or shape work. -->

## Backlog
<!-- Not-yet-scheduled work, grouped. Durable deferrals live here (or Open decisions), never under Now —
     the active block is overwritten as work moves, so parked items get lost. -->

## Done
<!-- Current + last 1–2 phases only — prune older (see header). -->
